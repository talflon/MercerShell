for (type in ['trust', 'key']) {
    applicationDefaultJvmArgs += [
        "-Djavax.net.ssl.${type}Store=${rootProject.file(keystoreProperties['storeFile'])}",
        "-Djavax.net.ssl.${type}StorePassword=${keystoreProperties['storePassword']}",
    ]
}
